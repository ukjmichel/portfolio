import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  fonts: {
    heading: 'ZenKakuGothicNew-Regular',
  },
  // You can add more theme customizations here if needed
});

export default theme;
